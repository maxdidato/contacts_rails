= form_tag contacts_path, :method => 'get', :remote => true do |f|
  %p
    = text_field_tag :search, params[:search]
    = submit_tag "Search", :name => nil
    = link_to 'remove filter', contacts_path, :remote => true